management:
  endpoint:
    restart:
      enabled: true
    health:
      show-details: always  
  endpoints:
      web:
        exposure:
            include: '*'
spring:
  cloud:
    kubernetes:
      config:
        namespace: ${KUBERNETES_NAMESPACE}
        name: ${spring.application.name}
