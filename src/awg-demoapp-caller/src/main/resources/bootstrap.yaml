spring:
  application:
    name: awg-demoapp-caller
  cloud:
    kubernetes:
      enabled: false
      config:
        enabled: false
      secrets:
        enabled: false
logging:
  level:
    org:
      springframework:
        cloud:
          kubernetes: debug
